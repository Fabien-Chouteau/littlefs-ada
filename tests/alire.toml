name = "tests"
description = "Shiny new project"
version = "0.1.0-dev"

executables = ["tests"]

[[depends-on]]
littlefs = "*"

[[pins]]
littlefs = { path = "../" }

[configuration.values]
littlefs.No_Malloc = false
